# Copy to /etc/gen-automation/migration.env with mode 0600 and root ownership.
# Use a dedicated schema-owner migration role. Never use the runtime or RDS
# master role, and never mount this file into the long-running application.
GEN_AUTOMATION_DATABASE_URL=
