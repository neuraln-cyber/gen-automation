# Copy to /etc/gen-automation/deploy.env with mode 0600 and root ownership.
# Every image must be an immutable registry/repository@sha256:<64 lowercase hex> reference.
GEN_AUTOMATION_CONTROL_PLANE_MEGA_IMAGE=
GEN_AUTOMATION_PATREON_BROWSER_IMAGE=
GEN_AUTOMATION_NGINX_IMAGE=
GEN_AUTOMATION_CADDY_IMAGE=
